import { NavBar } from "../NavBar";
import { HeaderWrapper } from "./styled";

export const Header = () => {
    return (
        <HeaderWrapper>
            <NavBar />
        </HeaderWrapper>
    );
};
